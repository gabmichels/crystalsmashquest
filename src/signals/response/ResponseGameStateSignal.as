package signals.response {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResponseGameStateSignal extends Signal implements ISignal{
		public function ResponseGameStateSignal() {
			super ();
		}
	}
}
