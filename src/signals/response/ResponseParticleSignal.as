package signals.response {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResponseParticleSignal extends Signal implements ISignal{
		public function ResponseParticleSignal() {
			super ();
		}
	}
}
