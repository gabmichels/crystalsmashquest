package signals.response {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResponseCrystalsSignal extends Signal implements ISignal{
		public function ResponseCrystalsSignal() {
			super ();
		}
	}
}
