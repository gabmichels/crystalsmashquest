package signals.response {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResponseResetCrystalSignal extends Signal implements ISignal{
		public function ResponseResetCrystalSignal() {
			super ();
		}
	}
}
