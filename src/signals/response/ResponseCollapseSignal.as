package signals.response {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResponseCollapseSignal extends Signal implements ISignal{
		public function ResponseCollapseSignal() {
			super ();
		}
	}
}
