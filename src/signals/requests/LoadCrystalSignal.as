package signals.requests {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class LoadCrystalSignal extends Signal implements ISignal{
		public function LoadCrystalSignal() {
			super ();
		}
	}
}
