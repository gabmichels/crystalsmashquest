package signals.requests {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class RequestGridSignal extends Signal implements ISignal{
		public function RequestGridSignal() {
			super ();
		}
	}
}
