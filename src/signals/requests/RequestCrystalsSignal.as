package signals.requests {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class RequestCrystalsSignal extends Signal implements ISignal{
		public function RequestCrystalsSignal() {
			super ();
		}
	}
}
