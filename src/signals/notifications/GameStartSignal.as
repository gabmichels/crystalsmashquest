package signals.notifications {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class GameStartSignal extends Signal implements ISignal{
		public function GameStartSignal() {
			super ();
		}
	}
}
