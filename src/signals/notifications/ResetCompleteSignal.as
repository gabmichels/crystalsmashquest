package signals.notifications {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class ResetCompleteSignal extends Signal implements ISignal{
		public function ResetCompleteSignal() {
			super ();
		}
	}
}
