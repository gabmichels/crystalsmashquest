package signals.notifications {
	import org.osflash.signals.ISignal;
	import org.osflash.signals.Signal;

	public class CollapseCompleteSignal extends Signal implements ISignal{
		public function CollapseCompleteSignal() {
			super ();
		}
	}
}
